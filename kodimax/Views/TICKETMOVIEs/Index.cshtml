@model IEnumerable<kodimax.Models.TICKETMOVIE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.movie.NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sala.NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CANT_BUTACA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TYPE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.movie.NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sala.NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CANT_BUTACA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TYPE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
